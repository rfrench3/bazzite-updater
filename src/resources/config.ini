# leave a command empty to disable it. Anything but system update will also become hidden
[Commands]
systemUpdateCommand=systemctl start uupd-manual.service --wait
systemRollbackCommand=bazzite-rollback-helper rollback -y

[AboutInfo]
iconPath=./Tux.png
# modify the OS KAboutData object in ./OsKAboutData.json
