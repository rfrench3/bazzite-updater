# Configs

For users (or distributors) that wish to make use of Bazzite Updater on a different distro, search for your distro.config.yml and use it to replace the original located at `/etc/bazzite-updater/config.ini`. These configs are minimal and can be used as reference for creating a more in-depth integration with your system.
